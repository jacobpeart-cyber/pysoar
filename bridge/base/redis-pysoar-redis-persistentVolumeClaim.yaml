#! redis-pysoar-redis-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: redis-pysoar-redis
    namespace: pysoar
    labels:
        com.docker.compose.service: redis
        com.docker.compose.volume: pysoar-redis
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
